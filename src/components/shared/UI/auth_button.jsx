const AuthButton = ({ text }) => {
  return (
    <button className="bg-Green100 rounded-3xl text-white  text-2xl h-14 justify-center content-center">
      {text}
    </button>
  );
};

export default AuthButton;
